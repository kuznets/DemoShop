const User = require('../models/user');

/**
 * routes:
 * 
 */

module.exports = {

  /**
  * GET /profile
  * Find user from db and put it to locals.
  * @method findOneUser
  * @return
  */
  findOneUser(req, res, next) {
    //TODO: findOneUser
    next();
  }
};